/** Stable bundled identities for collisions or intentional filename changes. */
export const bundledCatalogOverrides: Record<string, string> = {
  // Keep intentional renames here: 'current-file.osz': 'previous_slug'.
};
